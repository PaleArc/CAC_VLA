# External repositories

`external/oat` is the pinned PaleArc OAT fork submodule. OAT-specific changes
are maintained in that fork and referenced here by an immutable commit.

LIBERO-Plus can be cloned separately and selected with `LIBERO_PLUS_ROOT`. The
evaluation client also checks `external/libero-plus` as a convenience default.
